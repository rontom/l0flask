// document ready$( document ).ready(function() {    // initial load    var unix_time = $('#clndr_datetime').val();    get_ajax_posts(unix_time);});// get postsfunction get_ajax_posts(unix_time) {  // posts  posts_url = '/ajax/p';  $.ajax({      type: 'GET',      dataType: "html",      url: posts_url,      data: {date_time: unix_time },      success: function(data) {         $('#posts').html(data);        set_locale_timestamps("p_ts_", "calendar");                // write selected date        var m = moment.unix(unix_time);        var html = m.format('dddd') + " " + m.format('DD') + " " + m.format('MMMM');        html = html;        $('#selected_post_date').html(html);      },      contentType: "application/html",      dataType: 'html'  });}