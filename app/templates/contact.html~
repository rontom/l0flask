{% extends "layout.html" %}
{% block content %}
{% include "head.html" %}

  {% for m in form.cmessage.errors %}
    <div class="flash-error">{{ m }}</div>
  {% endfor %}
  {% if msg %}
    <div class="flash-success">{{ msg }}</div>
  {% else %}

    <div class="content">
      <div class="input-line">
        <div class="label-item"><pre>联系</pre></div>
      </div>
      <div class="clearfix"></div>
    </div>

    <div class="clearfix"></div>

    {% if user != None %}
      <form role="form" action="/contact" method="post" id="contact-form" class="form-horizontal">
      <div class="content">  
      <div class="post-line" id="contact-message">
        <div class="label-item">信息</div>
        <div class="text-item">
          <textarea name="cmessage" id="cmessage" placeholder="输入你想写的信息" class="form-control" required="required" value=""></textarea>
        </div>
        <div class="label-item"></div>
        <div class="text-item"><button type="submit" id="btn-submit" class="btn btn-default navbar-btn">提交</button></div>
      </div>
      <div class="clearfix"></div>
      </div>
      {{ form.hidden_tag() }}
      </form>
    {% else %}
      <div class="content">  
        <a href="/register">创建一个用户</a> 来提交信息!
		 <br/>
		  <span>此外你可以联系<a href='mailto:rontomai@gmail.com'>rontomai#gmail.com</a>把#改成@便可</span>
        <div class="clearfix"></div>
      </div>
    {% endif %}
    
{% endif %}

{% endblock %}
